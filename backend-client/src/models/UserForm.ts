export type UserForm = {
    cpf                 ?: string,
    birthDate           ?: string,
    email               ?: string,
    name                ?: string,
    phone               ?: string,
    password            ?: string,
    confirmationPassword?: string
}